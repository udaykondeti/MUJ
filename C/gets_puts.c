#include <stdio.h>

int main() {
    char inputString[100];

    /* Read a line of characters using gets() */
    gets(inputString);

    /* Display the input using puts() */
    puts(inputString);

    return 0;
}
